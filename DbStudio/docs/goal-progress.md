# 当前目标验收记录

用户目标：完成项目与 SQL Server 数据库工具；提供免登录且不可修改的分享网址并支持切换展示形式；明确项目隔离；可以修改项目信息。

## 已实现并验证

| 要求 | 当前证据 |
| --- | --- |
| 项目信息编辑 | 工作台顶部项目设置；浏览器修改测试项目名称和说明成功，版本递增 |
| 项目隔离 | 普通成员只列出所属项目；跨项目保存、导出、DDL、成员和审计均在服务测试中被拒绝 |
| 模块维护 | 可创建空模块并在侧栏显示 0 张表；重命名同步更新表归属 |
| 数据库连接 | 项目归属、密码加密、连接版本；SQL Server 实际连接测试通过 |
| 反推 | 真实系统目录读取 2 张测试表；主键、降序索引、精度、FK/CHECK 保留；预览合并保留已有 ID |
| 比对与同步 | DacFx 创建与增量部署通过，再次比对无差异；新增字段保留已有业务行 |
| 执行保护 | 完整数据库名确认、单次计划、旧项目版本和目标结构漂移拦截；默认拒绝含数据表的删除；显式删除保留既有视图 |
| 匿名只读分享 | 无 Cookie HTTP GET 返回 200；项目 API 返回 401；页面没有编辑会话；撤销后 GET 返回 404；有效期和伪造令牌检查通过 |
| 分享形式 | 浏览器验证表格/卡片切换、搜索入口、外键跳转；截图 share-cards.png |
| 发布包 | Release publish 成功；独立发布目录启动，登录和 CSS 200、匿名项目 API 401、无效分享 404 |
| 现有数据保护 | 升级前用 SQLite backup API 备份；升级后项目 Id、Revision、Document 与备份完全一致；未给真实项目创建连接或分享 |

最终自动检查：160 项通过，包含真实 SQL Server LocalDB 集成检查和匿名页面渲染检查。构建 0 警告、0 错误；dotnet format 验证通过。

实际预览：http://127.0.0.1:5188/ 。原项目仍为 99 张表、r6。

备份：`App_Data/before-project-isolation-20260907-221015.db`。

## 软件交付与部署边界

已交付创建免登录只读分享网址的功能、独立发布包和部署说明。当前预览仅监听回环地址，不能宣称公网分享已经上线。服务器、内网地址或公网域名尚未指定；实际上线另行部署、配置 PublicBaseUrl，并从另一台机器验证访问。

此前将“支持分享网址”直接解释为“现在必须完成公网部署”，并将整个开发目标标记为等待，属于范围判断错误。未指定部署环境不阻塞本地功能交付与验收；发布包也不等于已经上线。部署步骤见 deployment.md。

## 已知范围和运行说明

SQL Server 工具针对当前关系表设计模型；不支持无损表达的高级特性会在反推中提示。完整范围写在 README，不将其默默丢弃后导入。

用户原有 Visual Studio 的 5159 调试实例未停止；若使用该实例，需重新构建/启动以加载新版，当前验收针对 5188 新预览。

测试用 5190/5191 服务已结束，不使用用户业务库进行任何同步测试。

## 连接管理复核

继续目标时发现连接管理缺少删除入口，现已补齐刷新、删除确认、版本检查及删除后的计划失效。新增检查覆盖跨项目连接 ID、只允许管理者修改/删除、撤权、加密存储、密码不进入备份/审计、旧版本误删及旧编辑页恢复已删除连接。

真实 SQL Server 检查另确认：删除配置后旧计划被拒绝，实际库中的业务表及数据仍然存在。最新测试库为 `DbStudioIntegration_ca7e524228ea`，只用于独立测试。

## 最终分享复核（2026-09-08）

修复搜索后点击外键时目标表被原搜索条件隐藏的问题：外键跳转清空搜索，表格/卡片切换保留搜索。两种形式补全主键名称及聚集方式、键列升降序、字段排序规则，卡片补全计算列持久化状态。

新增 6 项实际 Razor HTML 渲染检查，验证以上信息、外键跳转目标、文本编码和无编辑运行时。所有 160 项检查通过；最新 SQL Server 测试库为 `DbStudioIntegration_9e484e0e132f`。测试运行在独立数据目录，没有修改真实项目。

最终发布包在独立测试目录启动后，再次用无 Cookie HTTP 请求验证表格与卡片均返回 200，包含自定义主键及 DESC；无编辑运行时，响应 no-store/no-referrer。匿名项目 API 为 401，撤销链接后为 404。测试服务与专用 LocalDB 已停止。

5188 已更新至最新版并返回 200；匿名项目 API 仍为 401。真实项目 Id、Revision、Document 再次与升级前备份逐项比较完全一致，真实连接和分享数量均为 0。格式检查通过，构建及发布无警告、无错误。软件开发目标完成，可以从工作台验收；外部环境部署仍未执行。
